{1}type rur = (a1,a2,a3,a4,a5,a6,a7,a8);

{2} mas = array[rur]of integer;

{3} mas1 = array[(b1,b2,b3,b4,b5,b6,b7,b8)] of integer;

{4}var a:mas; t:rur; b:mas1;

begin

{5} for t:=a8 to a1 do begin

       {6} a[t]:=random(10); write(a[t],' ')

                       end;
  readln;
writeln;

{7} b[b1]:=1;

{8} write(a[a1]>b[b1]);

    t:=a4;

{9} while t<=a7 do begin

                   write(ord(t),' ');

              {10} t:=pred(t)

                   end;

readln;

end.
